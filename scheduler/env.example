# Calendar selection (choose ONE; do not set both)
# Use a direct Google Calendar ID OR a sheet key that maps via scheduler/config.json
CALENDAR_ID=c_…@group.calendar.google.com
# SHEET_KEY=A

# YouTube live control
# Use the ID of the existing YouTube Live Stream you want to target (preferred)
# Alternatively, some workflows may use a stream key; see scheduler/README.md for details
YOUTUBE_STREAM_ID=YOUR_STREAM_ID

# Path to the OAuth desktop client JSON used to authorize YouTube actions
YOUTUBE_OAUTH_CREDENTIALS=C:\\path\\to\\desktop-client.json

# OBS auto-launch + profile/collection names
# Set to your local OBS install path on Windows
OBS_EXE=C:\\Program Files\\obs-studio\\bin\\64bit\\obs64.exe
# Must match an existing OBS profile name on the machine
OBS_PROFILE=Untitled
# Must match an existing OBS scene collection name on the machine
OBS_COLLECTION=Static Game Stream

# OBS websocket (optional: used by stop script to stop stream + quit cleanly)
# Host/port are fixed in the stop script (127.0.0.1:4455). Set only the password.
# If unset, the stop script falls back to window-close behavior.
OBS_WEBSOCKET_PASSWORD=your_password_here

# Google service account creds for Calendar read-only
# The service account must have Viewer access to the target calendar
GOOGLE_APPLICATION_CREDENTIALS=C:\\path\\to\\streaming.key.json

# Notes:
# - Keep this file and JSON credentials out of source control.
# - OBS camera inputs are configured within OBS and are not managed by this repository.

